# Compose-only, non-secret development defaults are built into docker-compose.yml.
# Copy this file to infra/.env only when overriding them; never commit real secrets.
POSTGRES_PASSWORD=personal_growth_agent_dev
POSTGRES_HOST_PORT=5433
BACKEND_HOST_PORT=8000
FRONTEND_HOST_PORT=3000

# Provider keys remain in backend/.env (copied from backend/.env.example).
NPM_REGISTRY=https://registry.npmjs.org
